<div class="inv-list stash center-text">
  <label>Your Stash</label>

    <ul class="item-list">
      {{#each model as |item|}}
        {{inventory-item item=item action="withdrawItem"}}
      {{/each}}
    </ul>
</div>
