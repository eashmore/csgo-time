<div class="row center-text">
  <h2>Total Value: ${{totalValue}}</h2>
</div>

<div class="inv-list stash center-text">
  <label>Your Stash</label>

  <ul class="item-list">
    {{#each model as |item|}}
      {{inventory-item item=item action="betItem"}}
    {{/each}}
  </ul>
</div>
