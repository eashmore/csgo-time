<div class="center-text">

  {{#if model.bet}}
    <br>
    <label>Total Value:</label>
    <p class="prize-pool" >${{model.bet.totalValue}}</p>
    <br>

    <div class="inv-list bet-show">
      <ul>
        {{#each model.betItems as |item|}}
          {{inventory-item item=item}}
        {{/each}}
      </ul>
    </div>

  {{else}}
    It looks like you haven't placed a bet yet.<br>
    Don't worry, there might still be time!<br>
  {{/if}}

</div>
